# szl-pin
Estate pinning: one SHA-256 hash commits to every repo head in the frontier. Deterministic, order-independent, offline-verifiable. pin_diff names every moved/added/removed head — drift is never silent. Fail-closed SHA validation.
